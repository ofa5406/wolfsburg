# Web tool lives in its own repository — see README (annestasiia/wolfsburg-activity-map)
wolfsburg-activity-map/

# Dependencies / build artifacts
node_modules/
dist/
build/
.vite/

# The InfAU submission package is pure build output — ~900 MB of copied,
# built and downsized files, and exactly what gets uploaded to Nextcloud.
# The tooling that produces it lives in submission-tools/ and IS tracked,
# including materials-web/ so site/materials/ can be rebuilt from this repo
# alone. The studio's brief is in briefs/infau-final-submission/.
final submission/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Rhino backups / temp
*.3dmbak
*.3dm.rhl
*.rhl

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*~
.vscode/
.idea/

# Local Netlify folder
.netlify
